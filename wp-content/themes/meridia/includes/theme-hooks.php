<?php
/**
 * Theme hooks.
 *
 * @package Meridia
 * @since 1.0.0
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit( 'Direct script access denied.' );
}

/**
 * Head top
 */
function meridia_head_top() {
	do_action( 'meridia_head_top' );
}

/**
 * Head bottom
 */
function meridia_head_bottom() {
	do_action( 'meridia_head_bottom' );
}

/**
 * Header before
 */
function meridia_header_before() {
	do_action( 'meridia_header_before' );
}

/**
 * Header after
 */
function meridia_header_after() {
	do_action( 'meridia_header_after' );
}

/**
 * Body top
 */
function meridia_body_top() {
	do_action( 'meridia_body_top' );
}

/**
 * Body bottom
 */
function meridia_body_bottom() {
	do_action( 'meridia_body_bottom' );
}

/**
 * Primary content top
 */
function meridia_primary_content_top() {
	do_action( 'meridia_primary_content_top' );
}

/**
 * Primary content bottom
 */
function meridia_primary_content_bottom() {
	do_action( 'meridia_primary_content_bottom' );
}


/**
 * Entry content top
 */
function meridia_entry_content_top() {
	do_action( 'meridia_entry_content_top' );
}

/**
 * Entry content bottom
 */
function meridia_entry_content_bottom() {
	do_action( 'meridia_entry_content_bottom' );
}

/**
 * Page section before
 */
function meridia_page_section_before() {
	do_action( 'meridia_page_section_before' );
}

/**
 * Page section after
 */
function meridia_page_section_after() {
	do_action( 'meridia_page_section_after' );
}

/**
 * Search results section before
 */
function meridia_search_results_section_before() {
	do_action( 'meridia_search_results_section_before' );
}

/**
 * Search results section after
 */
function meridia_search_results_section_after() {
	do_action( 'meridia_search_results_section_after' );
}

/**
 * Archive section before
 */
function meridia_archive_section_before() {
	do_action( 'meridia_archive_section_before' );
}

/**
 * Archive section after
 */
function meridia_archive_section_after() {
	do_action( 'meridia_archive_section_after' );
}

/**
 * 404 section before
 */
function meridia_404_section_before() {
	do_action( 'meridia_404_section_before' );
}

/**
 * 404 section after
 */
function meridia_404_section_after() {
	do_action( 'meridia_404_section_after' );
}


/**
 * Footer before
 */
function meridia_footer_before() {
	do_action( 'meridia_footer_before' );
}

/**
 * Footer after
 */
function meridia_footer_after() {
	do_action( 'meridia_footer_after' );
}